/**
 * //interface z abstrakcyjna metoda
 */

package snake.domain;

import java.awt.Graphics2D;

public interface Drawable {
	/**
	 * // rysowanie (renderowanie) obiektu na ekranie.
	 * @param g2
	 */
	 void paint(Graphics2D g2); // rysowanie (renderowanie) obiektu na ekranie.
}
